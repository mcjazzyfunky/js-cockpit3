// --- enums ---------------------------------------------------------

enum LabelPosition {
  Above = 'above',
  Beside = 'beside'
}

// === exports =======================================================

export default LabelPosition
