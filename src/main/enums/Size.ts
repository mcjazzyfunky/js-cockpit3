// --- enums ---------------------------------------------------------

enum Size {
  Compact = 'compact',
  Default = 'default',
  Large = 'large'
}

// --- exports -------------------------------------------------------

export default Size
